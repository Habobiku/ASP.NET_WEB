@model LoginViewModel

@{
    ViewData["Title"]= "Sing in";
}
<form class="login" action="/login" method="post">
    <h1>👋 Welcome!</h1>
    <label class="pure-material-textfield-outlined">
        <input placeholder="Email" type="email" name="Email" required value="@Model.Email">
        <div class = "error">@Html.ValidationMessageFor(m=>m.Email)</div>
    </label>
    <label class="pure-material-textfield-outlined">
        <input placeholder="Password" name="Password" type="password" required>
    </label>
    <label class="pure-material-checkbox">
        <input type="checkbox">
        <span>Remember me</span>
    </label>
    <button class="pure-material-button-contained" type="submit">Login</button>
</form>